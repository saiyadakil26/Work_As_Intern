==> Operators

    => Query and Projection Operators
        * comparision operator *
            > $eq
            > $ne
            > $gt
            > $gte
            > $lt
            > $lte
            > $in
            > $nin
        * Logical Operator *
            > $not
            > $and
            > $or
            > $nor
        * Element Query Operator *
            > $exist
            > $type
        * Eveluation Query Operator *
            > $expr
            > $jsonSchema
            > $mod
            > $regex
            > $text
            > $where
        * Geospatial Query Operator *
            > $geoIntersects
            > $geoWithin
            > $near
            > $nearSphere
            > $box
            > $center
            > $centerSphere
            > $geometry
            > $maxDistance
            > $minDistance
            > $polygon
        * Array Query Operator *
            > $elemMatch
            > $all
            > $size
        * Bitwise Query Operator *
            > $bitsAllClear
            > $bitsAllSet 
            > $bitsAnyClear
            > $bitsAnySet  
        * Projection Operator *
            > $
            > $elemMatch 
            > $slice
        * Miscellaneous Query Operator *
            > $comment
            > $rand 
            > $natural  

    => Update Operators
        * Field Update Operators *
            > $currentDate
            > $inc
            > $min
            > $max
            > $mul
            > $rename
            > $set
            > $setOnInsert
            > $unset
        * Array Update Operators *
            > $
            > $[]
            > $[]
            > $max
            > $mul
            > $rename
            > $set
            > $setOnInsert
            > $unset